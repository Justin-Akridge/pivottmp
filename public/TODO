490

when i extract marker and path locations I will then remove all points from point cloud that are outside of the pole line. I will then convert it with the potree converter. so that all the irrelevant points will be excluded from the lidar.
